'use strict';

/**
 * user-information-organisation service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::user-information-organisation.user-information-organisation');
