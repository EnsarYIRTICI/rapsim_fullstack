'use strict';

/**
 * user-information-organisation router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::user-information-organisation.user-information-organisation');
